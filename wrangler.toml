# Cloudflare Pages — projeto do site Norma Contábil
# Mesmo mecanismo do advisor-tips-website: Pages conectado ao GitHub (deploy no push).
# Site estático puro (sem build): o diretório de saída é a própria raiz do repositório.
name = "normacontabil-site"
pages_build_output_dir = "."
compatibility_date = "2024-11-01"
